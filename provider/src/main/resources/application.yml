
#端口号
server:
  port: 8001
#服务名
spring:
  application:
    name: xiaoyu-provider
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8850/nacos

#springboot监控暴漏端口
management:
  endpoints:
    web:
      exposure:
        include: "*"
